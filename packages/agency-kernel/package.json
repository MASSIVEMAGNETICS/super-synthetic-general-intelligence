{
  "name": "@massivemagnetics/aetherling-agency-kernel",
  "version": "0.1.0",
  "private": true,
  "description": "Verified AetherForge agency vertical slice: Informatron → Chronos → options → Ethica approval → restricted execution → trace → successor state.",
  "type": "commonjs",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run clean && tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "test:mict": "npm run build && node --test dist/tests/mict.test.js"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
