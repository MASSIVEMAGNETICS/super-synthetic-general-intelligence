{
  "name": "aetherforge",
  "version": "0.1.0",
  "private": true,
  "description": "AetherForge — Autonomous Creation Platform and Agent Operating System",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:agency-kernel": "npm run build --workspace @massivemagnetics/aetherling-agency-kernel",
    "build": "npm run build:backend && npm run build:frontend && npm run build:agency-kernel",
    "test:backend": "cd backend && npm test",
    "test:agency-kernel": "npm run test --workspace @massivemagnetics/aetherling-agency-kernel",
    "test": "npm run test:backend && npm run test:agency-kernel",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "sim": "cd simulation && npx ts-node src/index.ts"
  },
  "workspaces": [
    "backend",
    "frontend",
    "simulation",
    "packages/*"
  ]
}
